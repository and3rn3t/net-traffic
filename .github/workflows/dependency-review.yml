# Calls the shared reusable dependency-review workflow (and3rn3t/.github).
name: Dependency Review

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  dependency-review:
    uses: and3rn3t/.github/.github/workflows/reusable-dependency-review.yml@v1
